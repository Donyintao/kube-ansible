###
# kubernetes scheduler config
 
# default config should be adequate
###
 
# Add your own!
KUBE_SCHEDULER_ARGS="--address=0.0.0.0 \
                     --leader-elect=true \
                     --log-dir={{ KUBE_LOGS_PATH }}/kube-scheduler \
                     --kubeconfig={{ KUBE_HOME_PATH }}/etc/kube-scheduler.kubeconfig"
